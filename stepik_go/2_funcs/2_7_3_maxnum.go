/*

https://stepik.org/lesson/229321/step/4?auth=login&unit=201907

Дана строка, содержащая только арабские цифры. 
Найти и вывести наибольшую цифру.

Входные данные

Вводится строка ненулевой длины. Известно также, что длина строки 
не превышает 1000 знаков и строка содержит только арабские цифры.

Выходные данные

Выведите максимальную цифру, которая встречается во введенной строке.

Sample Input:
1112221112

Sample Output:
2

Test:
 "1112221112" | go run 2_7_3_maxnum.go
*/

package main

import "fmt"

func main() {
    var input string
    fmt.Scan(&input)
    
    // Начальное значение максимальной цифры - первый символ
    maxDigit := input[0]
    
    // Проходим по всем символам и ищем максимальную цифру
    for i := 1; i < len(input); i++ {
        if input[i] > maxDigit {
            maxDigit = input[i]
        }
    }
    
    // Печатаем результат, преобразуя байт в строку
    fmt.Printf("%c", maxDigit)
}